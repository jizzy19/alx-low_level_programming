#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: Argument vector
 * Return: Always
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
